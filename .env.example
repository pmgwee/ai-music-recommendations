NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
SUPABASE_SERVICE_ROLE_KEY=
# SP-0 only — server GLM key (BYOK replaces this in SP-2)
ZAI_API_KEY=
ZAI_BASE_URL=
GLM_MODEL=
# SP-0 only — fixed dev user id (a UUID the controller creates in auth.users).
# The proof routes bind the TrackStore to the service-role admin client + this
# id because auth/cookie/RLS is SP-1's scope. SP-1 deletes this var.
MUSIC_DEV_USER_ID=
